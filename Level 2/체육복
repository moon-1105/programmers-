global arr
def include(ele):
    print(arr)
    if ele in arr:
        return False
    else:
        return True
    
def solution(n, lost, reserve):
    answer = n-len(lost)
    #lost = [1,2,3,4]
    #reserve = [1, 2,5]
    global arr
    arr = lost
    arr2 = []
    for ele in filter(include, reserve):
        arr2.append(ele)
        #print(ele)
    #reserve.sort()
    #print("=>",arr2)
    """
    여분을 갖고있는 학생이 도난 당한 경우
    [lost]와 [reserve]의 중복되는 번호를 제거
    filter로 걸러진 [reserve]의 순서를 오름차순으로 정렬
'while'문을 사용해서 여분을 갖고있는 학생이 자신의 앞번호-뒷번호 순으로 탐색 후 빌려줌
성공적으로 빌려준 경우 answer+1
제출
    """
    print(arr)
    while len(arr2)!=0:
        now = arr2.pop(0)
        print("=>",now, lost)
        print(now-1 in lost)
        print(now+1 in lost)
        if now-1 in lost:
            answer += 1
        elif now+1 in lost:
            answer+=1
    return answer
