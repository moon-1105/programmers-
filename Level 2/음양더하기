def solution(absolutes, signs):
    answer = 0
    N = len(signs)
    for i in range(0,N):
        if signs[i]:
            answer += absolutes[i]
        else:
            answer -= absolutes[i]
    return answer
