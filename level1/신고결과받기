def solution(id_list, report, k):
    answer = []
    report_set = {}
    for id in id_list:
        report_set[id] = 0
    for r in report:
        report_set[id] += 1
    banned = []
    for r in report_set:
        if report_set(r) >= k:
            print(r)
        
    return answer
