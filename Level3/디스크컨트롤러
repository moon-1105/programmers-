import heapq
# 평균을 가장 줄이는 방법으로 처리하면 평균이 얼마가 되는지
class job:
    def __init__(self, s, d):
        self.start = s 
        self.during = d
        self.end = 0
        self.done = False
    def __lt__(self, other):
        if self.during >= other.during:
            return False
        else:
            return True
    def __str__(self):
        return "["+str(self.start)+","+str(self.during)+"]"
    
def solution(jobs):
    answer = 0
    arr = []
    for ele in jobs:
        heapq.heappush(arr, job(ele[0],ele[1]))
    #정렬은 헀는데,, 요청이 있을때 없을때 나눠서 진행해야함..
    while arr:
        print(heapq.heappop(arr))
    
           
    return answer
