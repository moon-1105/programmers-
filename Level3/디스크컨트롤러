# 작업의 요청부터 종료까지 걸린 시간의 평균을 가장 줄이는 방법으로 처리하면 
# 평균이 얼마가 되는지
# sort 한다음에 짧은것 부터 수행하면 되나? 
def solution(jobs):
    answer = 0
    jobs.sort(key = lambda x:x[1])
    print(jobs)
    arr = []
    for job in jobs:
        now_time = job[1]
        arr.append([])
        
    return answer
